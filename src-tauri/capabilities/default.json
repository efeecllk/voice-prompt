{
  "$schema": "https://schemas.tauri.app/capabilities/schema.json",
  "identifier": "default",
  "description": "Default capabilities for Voice Prompt app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister",
    "store:default"
  ]
}
